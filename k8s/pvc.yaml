apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hermes-data
  namespace: hermes
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  # Autopilot 默认 StorageClass 为 standard-rwo(balanced PD)。
  # storageClassName: standard-rwo
